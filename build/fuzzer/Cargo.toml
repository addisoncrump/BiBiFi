[package]
name = "bibifi-fuzzer"
version = "0.1.0"
authors = ["Garrett Persyn <gpersyn@tamu.edu>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
# strictly do not depend on any bibifi libs; should be agnostic
